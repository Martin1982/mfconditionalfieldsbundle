{% block conditional_field_js_row %}
    <script type="text/javascript">
        {%- set debug = 'false' %}
        {%- if app.request.server.get('APP_ENV') == 'dev' %}
            {%- set debug = 'true' %}
        {%- endif %}
        if (typeof mfConditionalFields !== 'undefined') {
            mfConditionalFields('{{ form_id }}', {
                debug: {{ debug }}
            });
        }
    </script>
{% endblock %}